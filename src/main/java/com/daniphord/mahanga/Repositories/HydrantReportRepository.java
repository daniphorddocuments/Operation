package com.daniphord.mahanga.Repositories;

import com.daniphord.mahanga.Model.HydrantReport;
import org.springframework.data.jpa.repository.JpaRepository;

public interface HydrantReportRepository extends JpaRepository<HydrantReport, Long> {
}
